server {
  port = 8081
  host = "0.0.0.0"
}

database {
  path = "data/server.db"
  maxPoolSize = 10
  flushRate = 10
}

publishing {
  cpuStatsFrequency = 1000
  ramStatsFrequency = 1000
}

monitoring {
  sampleIntervalMs = 1000
}
