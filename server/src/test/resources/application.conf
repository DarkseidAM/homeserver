server {
    port = 8080
    host = "localhost"
}
database {
    path = "build/test-resolution.db"
    maxPoolSize = 10
    flushRate = 15
}
publishing {
    cpuStatsFrequency = 1000
    ramStatsFrequency = 1000
}
